# === Основные зависимости бота ===
python-telegram-bot==21.6
python-dotenv==1.0.1
aiosqlite==0.20.0
httpx==0.27.2

# === Обработка изображений и штрихкодов ===
Pillow>=10.4.0
pyzbar==0.1.9

# === Обработка текста и голоса ===
python-Levenshtein>=0.26.0
pymorphy2==0.9.1
SpeechRecognition>=3.10.0
pydub>=0.25.1

# === Кэширование ===
cachetools>=5.0.0

# === 🆕 НОВЫЕ: Научная аналитика и визуализация ===
pykalman>=0.9.5        # Для сглаживания веса (Фильтр Калмана)
pandas>=2.0.0          # Для работы с временными рядами (нужен для statsmodels)
statsmodels>=0.14.0    # Для теста причинности Грейнджера (Granger Causality)
matplotlib>=3.8.0      # Для генерации красивых графиков-картинок